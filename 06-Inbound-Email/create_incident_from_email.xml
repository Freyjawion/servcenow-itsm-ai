<?xml version="1.0" encoding="UTF-8"?>
<unload>
  <!-- 
    Inbound Email Action: Create Incident from Email
    When: Any email is received
    What: Creates a new incident record from the email content
    How: Uses ITSMEmailParser script include to extract fields
  -->
  <sys_inbound_email_action action="INSERT_OR_UPDATE">
    <sys_id>itsm_inbound_email_create_incident</sys_id>
    <name>Create Incident from Email</name>
    <active>true</active>
    <type>create</type>
    <table>incident</table>
    <action_order>100</action_order>
    <script><![CDATA[
(function runAction(/*GlideRecord*/ current, /*GlideEmailReader*/ email) {
    
    // Use the ITSMEmailParser to extract and set incident fields
    var parser = new ITSMEmailParser();
    var success = parser.parseAndCreateIncident(email, current);
    
    if (success) {
        gs.info('Inbound Email: Incident created from email - Subject: ' + email.subject);
        
        // Set additional fields specific to email creation
        current.contact_type = 'email';
        
        // The Business Rule "Set L1 Group Assignment" will auto-assign
        // the appropriate L1 group based on the category that was extracted
        
        return true;
    } else {
        gs.error('Inbound Email: Failed to create incident from email: ' + email.subject);
        return false;
    }
    
})(current, email);
    ]]></script>
    <description>Creates a new incident record from incoming emails. Extracts category, subcategory, urgency and other fields from the email content.</description>
  </sys_inbound_email_action>
</unload>
